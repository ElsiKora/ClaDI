import { unstable_TSDoc as TSDoc } from 'nextra/tsdoc'

# createFactory

*Defined in: `src/presentation/utility/create/factory.utility.ts`*

Helper to create a `BaseFactory` instance.

<TSDoc code={`import { createFactory } from '@elsikora/cladi'; export default createFactory`} /> 