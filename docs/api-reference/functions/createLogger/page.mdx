import { unstable_TSDoc as TSDoc } from 'nextra/tsdoc'

# createLogger

*Defined in: `src/presentation/utility/create/logger.utility.ts`*

Helper to create a `ConsoleLoggerService` instance.

<TSDoc code={`import { createLogger } from '@elsikora/cladi'; export default createLogger`} /> 