import { unstable_TSDoc as TSDoc } from 'nextra/tsdoc'

# hasFunctions

*Defined in: `src/application/utility/has-functions.utility.ts`*

Checks if an object or its nested properties contain any functions.

<TSDoc code={`import { hasFunctions } from '@elsikora/cladi'; export default hasFunctions`} /> 