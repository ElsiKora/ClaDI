import { unstable_TSDoc as TSDoc } from 'nextra/tsdoc'

# createRegistry

*Defined in: `src/presentation/utility/create/registry.utility.ts`*

Helper to create a `BaseRegistry` instance.

<TSDoc code={`import { createRegistry } from '@elsikora/cladi'; export default createRegistry`} /> 