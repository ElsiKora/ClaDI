import { unstable_TSDoc as TSDoc } from 'nextra/tsdoc'

# createContainer

*Defined in: `src/presentation/utility/create/container.utility.ts`*

Helper to create a `BaseContainer` instance.

<TSDoc code={`import { createContainer } from '@elsikora/cladi'; export default createContainer`} /> 