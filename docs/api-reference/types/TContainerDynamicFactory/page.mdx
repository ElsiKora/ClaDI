import { unstable_TSDoc as TSDoc } from 'nextra/tsdoc'

# TContainerDynamicFactory

*Defined in: `src/domain/type/container-dynamic-factory.type.ts`*

Represents a dynamic factory function used by the `IContainer`.

<TSDoc code={`import type { TContainerDynamicFactory, IContainerDynamicFactoryResolutionContext } from '@elsikora/cladi'; 
// Need context if referenced
export interface Context extends IContainerDynamicFactoryResolutionContext {}
export default TContainerDynamicFactory`} /> 