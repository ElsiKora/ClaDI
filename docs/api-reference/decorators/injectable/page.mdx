import { unstable_TSDoc as TSDoc } from 'nextra/tsdoc'

# @Injectable()

*Defined in: `src/presentation/decorator/injectable.decorator.ts`*

Decorator that marks a class as injectable and associates it with a specific container name. This allows the class to be automatically managed by the dependency injection system.

<TSDoc code={`import { Injectable } from '@elsikora/cladi'; export default Injectable`} /> 